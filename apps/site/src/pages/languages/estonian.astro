---
import LanguagesPage from "../../components/islands/LanguagesPage";
import { ESTONIAN_BLURB, ESTONIAN_TITLE } from "../../components/landing/LanguagesView";
import { SHARE_IMAGE_ALT } from "../../components/landing/LandingView";
import BaseLayout from "../../layouts/BaseLayout.astro";
import { pageI18n } from "../../lib/i18n";
import { localizedPath } from "../../lib/routes";

const locale = Astro.currentLocale ?? "en";
const i18n = pageI18n(locale);
const title = i18n._(ESTONIAN_TITLE);
const description = i18n._(ESTONIAN_BLURB);
const shareImageAlt = i18n._(SHARE_IMAGE_ALT);
const path = localizedPath("estonian", locale);

const structuredData = {
  "@context": "https://schema.org",
  "@type": "SoftwareApplication",
  name: "Lymi",
  applicationCategory: "EducationalApplication",
  operatingSystem: "Web",
  description,
  url: new URL(path, "https://lymi.app").toString(),
  inLanguage: locale,
  offers: { "@type": "Offer", price: "0", priceCurrency: "USD" },
};
---

<BaseLayout
  title={title}
  description={description}
  canonicalPath={path}
  localizedPage="estonian"
  shareImageAlt={shareImageAlt}
>
  <script
    slot="head"
    is:inline
    type="application/ld+json"
    set:html={JSON.stringify(structuredData).replaceAll("<", "\\u003c")}
  />
  <LanguagesPage client:load page="estonian" locale={locale} />
</BaseLayout>
